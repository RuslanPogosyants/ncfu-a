line-length = 100

exclude = [
    ".venv",
    "venv",
    "__pycache__",
    ".git",
    "data",
    "migrations",
]

[lint]
select = ["E", "F", "W"]
ignore = ["E501"]  # line too long — covered by formatter
